apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-follow-pvc
  namespace: databases
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
